import { createTheme } from '@mui/material';

export const theme = createTheme({
  typography: {
    fontFamily: 'inherit',
  },
  palette: {
    background: {
      default: '#035E7B',
    },
    primary: {
      main: '#035E7B',
      light: '#fff',
    },
    text: {
      primary: '#fff',
    },
  },
});
